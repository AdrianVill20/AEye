# AEye project dependencies
# Requires Python 3.13+
# Install with: pip install -r requirements.txt

PySide6>=6.11
opencv-python>=4.10
mediapipe>=0.10.35
numpy>=2.0
dlib-bin>=20.0
mysql-connector-python>=9.0

# Notes:
# - dlib-bin is a pre-built wheel (no Visual Studio C++ Build Tools needed).
# - This project expects the dlib landmark model file in eye_gaze/shape_predictor_68_face_landmarks.dat
# - MediaPipe model files are in head_pose/ (face_landmarker.task, pose_landmarker_heavy.task)
