# Python virtual environments
.venv/
venv/
env/

# Python caches / build artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# PyInstaller
*.spec

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# AEye gaze - per-user generated calibration data & trained models
# (each tester calibrates locally; don't share one person's calibration)
app_video/gaze/calibration_data.json
app_video/gaze/gaze_model.pkl
app_video/gaze/zone_data.json
app_video/gaze/zone_model.pkl

# AEye - large dlib face-landmark model (download locally; too big for git)
shape_predictor_68_face_landmarks.dat
